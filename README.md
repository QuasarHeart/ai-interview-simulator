# AI Interview Simulator

A real-time AI mock interview system with streaming interaction and asynchronous report generation.

## Overview

This project simulates a realistic interview environment using large language models.  
It supports real-time conversational interaction and generates structured evaluation reports after each session.

## Architecture

- Frontend: WebSocket-based real-time interaction
- Backend: Spring Boot (Java)
- ML Service: FastAPI (Python)
- Streaming for interview dialogue
- Asynchronous tasks for final report generation

## Development Rules

- Do NOT push directly to the `main` branch.
- Create your own feature branch for development.
- Submit a Pull Request (PR) to merge changes.
- Ensure the system builds and runs correctly before merging.
